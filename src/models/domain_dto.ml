(*
 * This file has been generated by the OCamlClientCodegen generator for openapi-generator.
 *
 * Generated by: https://openapi-generator.tech
 *
 * Schema Domain_dto.t : Domain plus verification records and status
 *)

type t = {
    id: string;
    user_id: string;
    (* Custom domain name *)
    domain: string;
    (* Verification tokens *)
    verification_token: string;
    (* Unique token DKIM tokens *)
    dkim_tokens: string list;
    (* If the domain is missing records then show which pairs are missing. *)
    missing_records_message: string option [@default None];
    (* Whether the domain has missing required records. If true then see the domain in the dashboard app. *)
    has_missing_records: bool;
    (* Whether domain has been verified or not. If the domain is not verified after 72 hours there is most likely an issue with the domains DNS records. *)
    is_verified: bool;
    (* List of DNS domain name records (C, MX, TXT) etc that you must add to the DNS server associated with your domain provider. *)
    domain_name_records: Domain_name_record.t list;
    (* The optional catch all inbox that will receive emails sent to the domain that cannot be matched. *)
    catch_all_inbox_id: string option [@default None];
    created_at: string;
    updated_at: string;
    (* Type of domain. Dictates type of inbox that can be created with domain. HTTP means inboxes are processed using SES while SMTP inboxes use a custom SMTP mail server. SMTP does not support sending so use HTTP for sending emails. *)
    domain_type: Enums.domaintype;
} [@@deriving yojson { strict = false }, show ];;

(** Domain plus verification records and status *)
let create (id : string) (user_id : string) (domain : string) (verification_token : string) (dkim_tokens : string list) (has_missing_records : bool) (is_verified : bool) (domain_name_records : Domain_name_record.t list) (created_at : string) (updated_at : string) (domain_type : Enums.domaintype) : t = {
    id = id;
    user_id = user_id;
    domain = domain;
    verification_token = verification_token;
    dkim_tokens = dkim_tokens;
    missing_records_message = None;
    has_missing_records = has_missing_records;
    is_verified = is_verified;
    domain_name_records = domain_name_records;
    catch_all_inbox_id = None;
    created_at = created_at;
    updated_at = updated_at;
    domain_type = domain_type;
}

